const functions = {
 /**
 @param { Javah } javah
 */
 queue:(name, key) => {

   (!name || !key) ? (return) : null;

   const \name = { ... };

   \name[key.name] = key.value;
   await \name[key];
   \name.slice(\name.indexOf(key.name));

 },
 resources:() => {

   packageMarket:() => {

   }

 },
 start:(buildName) => {

   (!buildName) ? throw new Error("Nothing to start.") : null;

   while(callback()) { return this };
   continue();

 }
}

module.exports = functions;

