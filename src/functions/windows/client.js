const Document = require('DocDesigner.js');
const ReplicatedExpressified = require('ReplicatedExpressified.js');

const Client = new Document();

const Event = new ReplicatedExpressified.EventParser()
const User = new ReplicatedExpressified.UserParser()

const Time = new Data.getTime() - this.launched();
const Data = ReplicatedExpressified.init();
const BackupData = this.backup();

 Expressified[user].routes.open("client", Dashboard);

